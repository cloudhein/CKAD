# Use a lightweight base image
FROM alpine:3.18

# Add a label for metadata (optional but good practice)
LABEL maintainer="ckad-candidate"

# Run a simple command to keep the container alive or print something
CMD ["echo", "CKAD Exam Prep"]